# Retrieved from wafpass-server: http://localhost:8000
# ID: WAF-SEC-010
id: WAF-SEC-010
pillar: security
severity: critical
type:
  - governance
  - configuration
description: |
  All AWS accounts MUST enforce MFA for all IAM users with console access. Root account usage MUST be prohibited for day-to-day operations. IAM password policy MUST meet minimum security requirements: minimum length of 14 characters, complexity requirements, password reuse prevention of at least 5 prior passwords, and maximum password age of 90 days. Service accounts MUST use IAM roles with temporary credentials rather than long-lived access keys wherever possible.
regulatory_mapping:
  - framework: 'ISO 27001:2022'
    controls:
      - 'A.8.2 – Privileged access rights'
      - 'A.8.5 – Secure authentication'
      - 'A.5.17 – Authentication information'
      - 'A.5.15 – Access control'
  - framework: 'ISO 27017'
    controls:
      - 'CLD.9.5 – Segregation in virtual computing environments'
  - framework: 'CIS Controls v8'
    controls:
      - 'CIS 5 – Account Management'
      - 'CIS 6 – Access Control Management'
  - framework: 'CSA STAR'
    controls:
      - 'CCM IAM-01 – Identity and Access Management Policy'
      - 'CCM IAM-02 – Strong Password Policy and Procedures'
  - framework: 'PCI DSS v4.0'
    controls:
      - 'Req 7 – Restrict access by business need to know'
      - 'Req 8 – Identify and authenticate access'
  - framework: 'SOC 2 Type II'
    controls:
      - 'CC6.1 – Logical and physical access controls'
      - 'CC6.2 – Provisioning and removal of access'
      - 'CC6.3 – Authentication mechanisms'
  - framework: 'COBIT 2019'
    controls:
      - 'DSS05.04 – Manage user identity and logical access'
  - framework: 'GDPR'
    controls:
      - 'Art. 32 – Security of processing'
      - 'Art. 5(1)(f) – Integrity and confidentiality'
  - framework: 'NIS2'
    controls:
      - 'Art. 21(2)(i) – Human resources security and access control'
  - framework: 'DORA'
    controls:
      - 'Art. 9 – Protection and prevention'
  - framework: 'EUCS (ENISA)'
    controls:
      - 'IAM-01 – Identity and access management'
      - 'IAM-02 – Authentication controls'
  - framework: 'BSI C5:2020'
    controls:
      - 'IDM-01 – Identity and access management policy'
      - 'IDM-02 – Authentication of users'
      - 'IDM-09 – Privileged access rights'
  - framework: 'IT-Grundschutz'
    controls:
      - 'ORP.4 – Identitäts- und Berechtigungsmanagement'
  - framework: 'TISAX'
    controls:
      - 'Information security – Identity and access management requirements'
  - framework: 'ANSSI SecNumCloud'
    controls:
      - 'Domain – Identity and access management'
  - framework: 'BIO'
    controls:
      - 'BIO – Logische toegangsbeveiliging (logical access control)'
  - framework: 'ENS High'
    controls:
      - 'op.acc.5 – Mecanismo de autenticación'
      - 'op.acc.7 – Gestión de privilegios'
  - framework: 'UK Cyber Essentials'
    controls:
      - 'CE.UAC – User access control'
  - framework: 'UK NCSC CAF'
    controls:
      - 'B2 – Identity and access control'
  - framework: 'UK GDPR'
    controls:
      - 'Art. 32 – Security of processing'
  - framework: 'NIST SP 800-53'
    controls:
      - 'AC-2 – Account Management'
      - 'IA-2 – Identification and Authentication'
      - 'IA-5 – Authenticator Management'
  - framework: 'NIST CSF 2.0'
    controls:
      - 'PR.AA-01 – Identities and credentials are managed'
      - 'PR.AA-03 – Users, services and hardware are authenticated'
  - framework: 'FedRAMP'
    controls:
      - 'AC-2, IA-2, IA-5 (Moderate/High baseline)'
  - framework: 'CMMC 2.0'
    controls:
      - 'AC.L2-3.1.1 – Limit system access to authorized users'
      - 'IA.L2-3.5.3 – Multifactor authentication'
  - framework: 'ASD Essential 8'
    controls:
      - 'E8.5 – Restrict administrative privileges'
      - 'E8.7 – Multi-factor authentication'
  - framework: 'IRAP'
    controls:
      - 'ISM – Identification and authentication'
  - framework: 'CCCS PBMM'
    controls:
      - 'AC-2, IA-2, IA-5 – Account and authenticator management'
  - framework: 'MAS TRM'
    controls:
      - 'Ch.6 – Access control'
  - framework: 'ISMAP'
    controls:
      - 'Identity and access management controls'
  - framework: 'FISC'
    controls:
      - 'Technical measures – User authentication and access control'
checks:
  - id: waf-sec-010.tf.aws.iam-password-policy-mfa
    engine: terraform
    automated: true
    title: 'AWS IAM account password policy must meet minimum security requirements'
    provider: aws
    description: 'The aws_iam_account_password_policy resource must enforce a minimum password length of 14, all character class requirements, a minimum of 5 passwords in reuse prevention history, and a maximum password age of 90 days.'
    expected: 'true'
    remediation: 'Define an aws_iam_account_password_policy resource with all required attributes set. Ensure minimum_password_length >= 14, all require_* flags are true, password_reuse_prevention >= 5, and max_password_age <= 90.\n'
    example:
      compliant: |
        resource "aws_iam_account_password_policy" "strict" {
          minimum_password_length        = 14
          require_uppercase_characters   = true
          require_lowercase_characters   = true
          require_numbers                = true
          require_symbols                = true
          allow_users_to_change_password = true
          max_password_age               = 90
          password_reuse_prevention      = 5
          hard_expiry                    = false
        }
        
      non_compliant: |
        resource "aws_iam_account_password_policy" "weak" {
          minimum_password_length      = 8
          require_uppercase_characters = false
          require_symbols              = false
          # Missing password_reuse_prevention and max_password_age
        }
        
    scope:
      block_type: resource
      resource_types:
        - aws_iam_account_password_policy
    assertions:
      - attribute: 'minimum_password_length'
        op: 'greater_than_or_equal'
        message: 'IAM password policy must require a minimum of 14 characters.'
        expected: 14
      - attribute: 'require_uppercase_characters'
        op: 'is_true'
        message: 'IAM password policy must require uppercase characters.'
      - attribute: 'require_lowercase_characters'
        op: 'is_true'
        message: 'IAM password policy must require lowercase characters.'
      - attribute: 'require_numbers'
        op: 'is_true'
        message: 'IAM password policy must require numbers.'
      - attribute: 'require_symbols'
        op: 'is_true'
        message: 'IAM password policy must require symbols.'
      - attribute: 'password_reuse_prevention'
        op: 'greater_than_or_equal'
        message: 'IAM password policy must prevent reuse of at least 5 prior passwords.'
        expected: 5
      - attribute: 'max_password_age'
        op: 'less_than_or_equal'
        message: 'IAM password policy must set maximum password age to 90 days or less.'
        expected: 90
    on_fail: violation
  - id: waf-sec-010.tf.aws.no-root-access-keys
    engine: terraform
    automated: true
    title: 'Root account must not have active access keys'
    provider: aws
    description: 'The AWS root account must not have active programmatic access keys. This cannot be fully checked in Terraform state but must be validated via AWS IAM credential report or Security Hub control IAM.4.'
    expected: 'true'
    remediation: 'Navigate to AWS IAM > Security Credentials for the root account and delete all access keys. Enable MFA on the root account. Restrict root account usage to only tasks that absolutely require it (e.g., account closure, enabling MFA for the first time).\n'
    example:
      compliant: |
        # Root access keys are removed via AWS Console – not representable in Terraform.
        # Verify with: aws iam get-account-summary | grep AccountAccessKeysPresent
        # Expected: "AccountAccessKeysPresent": 0
        
      non_compliant: |
        # Root account has active access keys visible in AWS IAM > Security Credentials.
        # aws iam get-account-summary output shows "AccountAccessKeysPresent": 1
        
    scope:
      block_type: resource
      resource_types:
        - aws_iam_user
    assertions:
      - attribute: 'name'
        op: 'not_equals'
        message: 'Root account must not be managed as a regular IAM user resource. Remove root access keys via AWS Console.'
        expected: 'root'
    on_fail: violation
